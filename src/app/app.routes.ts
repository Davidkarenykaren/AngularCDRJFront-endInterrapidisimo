import { Routes } from '@angular/router';
import { Cliente } from './cliente/cliente';

export const routes: Routes = [
     {
    path: '',
    component: Cliente,
    title: 'Cliente',
  }
  
  
];
